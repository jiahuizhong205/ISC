# ISC Project 2 — 任务分解文档

---

## 阶段一：项目初始化（全员参与）

| 编号 | 任务 | 详情 | 估计工时 | 负责人 | 完成标准 |
|---|---|---|---|---|---|
| P1-1 | 创建项目目录结构 | 按 README 结构创建所有目录，初始化 `__init__.py` | 0.5h | C | 目录结构与 README 一致，`python -c "import src"` 无报错 |
| P1-2 | 编写 requirements.txt 并安装依赖 | 锁定 numpy, scipy, pillow, opencv-python, matplotlib 版本 | 0.5h | C | `pip install -r requirements.txt` 全部安装成功 |
| P1-3 | 下载柯达图集 | 从 `https://r0k.us/graphics/kodak/` 下载全部 24 张 PNG 图像到 `data/kodak/` | 1h | A | `data/kodak/` 下有 24 张 .png 文件，每张尺寸 768×512 |
| P1-4 | 图集预处理 | 统一转为 YCbCr 色彩空间备用，写一个 `data/preprocess.py` 脚本 | 1h | A | 脚本可将任意输入图转为 Y/Cb/Cr 三通道 numpy array |

---

## 阶段二：源编码模块（成员 A）

### 2.1 编码器

| 编号 | 任务 | 详情 | 估计工时 | 负责人 | 完成标准 |
|---|---|---|---|---|---|
| A-1 | 8×8 分块函数 | 输入 Y/Cb/Cr 三个通道，分别按 8×8 分块，边缘不足时对称填充 | 1h | A | 输入 768×512 图像，输出 96×64 个 8×8 块，通过单元测试 |
| A-2 | 二维 DCT 变换 | 对每个 8×8 块做 2D-DCT，使用 `scipy.fft.dct` 或手动实现 | 1.5h | A | 与 `scipy.fft.dct` 结果误差 < 1e-6，通过单元测试 |
| A-3 | 量化表设计 | 参考 JPEG 标准亮度/色度量化表，提供可调 quality factor (1~100) | 1.5h | A | `set_quality(q)` 可动态调整量化步长，Q=50 等价于 JPEG 标准表 |
| A-4 | Zigzag 扫描 + 游程编码 | 将量化后 8×8 块按 zigzag 顺序展开，做 (run, value) 游程编码 | 2h | A | 输入量化后 DCT 块，输出 RLE 符号序列，编解码可逆无信息丢失 |
| A-5 | Huffman 编码 | 对 RLE 符号统计频率，构建 Huffman 树，编码为比特流；同时输出码表 | 2.5h | A | 编解码完全可逆，压缩率 > 50%（Q=50 时），通过单元测试 |

### 2.2 解码器

| 编号 | 任务 | 详情 | 估计工时 | 负责人 | 完成标准 |
|---|---|---|---|---|---|
| A-6 | 源解码器 | Huffman 解码 → 逆 RLE → 逆 Zigzag → 逆量化 → 逆 DCT → 合并 8×8 块 | 2h | A | 无信道损伤时，解码图像与原图逐像素误差 < 1（量化误差），PSNR > 35dB (Q=75) |
| A-7 | YCbCr 转 RGB | 解码后合并三个通道，转回 RGB 色彩空间，保存为 PNG | 0.5h | A | 输出的 PNG 可用任意图片查看器正常打开 |

### 2.3 测试

| 编号 | 任务 | 详情 | 估计工时 | 负责人 | 完成标准 |
|---|---|---|---|---|---|
| A-8 | 源编码单元测试 | 覆盖编码→解码完整链路，测试不同 Q 值下的 PSNR 和压缩率 | 2h | A | 至少 3 个 Q 值 (10, 50, 90) 的测试用例全部通过 |

**成员 A 合计工时：14h**

---

## 阶段三：信道编码模块（成员 B）

### 3.1 卷积码编码器

| 编号 | 任务 | 详情 | 估计工时 | 负责人 | 完成标准 |
|---|---|---|---|---|---|
| B-1 | 卷积码编码器 | 实现 (2, 1, 7) 卷积码，生成多项式使用 NASA 标准 (171, 133) | 2h | B | 输入 1000 bit，输出 2000 bit 编码序列，与已知参考输出一致 |
| B-2 | 尾部比特处理 | 编码末尾追加 K-1 个 0 将编码器归零 | 1h | B | 编码器末尾状态归零，通过单元测试 |

### 3.2 Viterbi 译码器

| 编号 | 任务 | 详情 | 估计工时 | 负责人 | 完成标准 |
|---|---|---|---|---|---|
| B-3 | Viterbi 硬判决译码 | 基于汉明距离的 Viterbi 算法，Trellis 图用 ACS 蝶形运算 | 3h | B | BSC ε=0.01 时 BER 降低至译码前的 1/10 以下 |
| B-4 | Viterbi 软判决译码 | 基于欧氏距离的 Viterbi，输入信号不做硬判 | 2.5h | B | BSC ε=0.05 时软判决 BER 优于硬判决 ≥ 1dB |
| B-5 | BEC 擦除处理 | 删除位置 LLR 置 0，其余正常译码 | 1.5h | B | BEC p=0.1 时仍可正确恢复 > 95% 比特 |

### 3.3 交织器

| 编号 | 任务 | 详情 | 估计工时 | 负责人 | 完成标准 |
|---|---|---|---|---|---|
| B-6 | 块交织器 | 实现 M×N 块交织/解交织，比特按行写入按列读出 | 1.5h | B | 交织→解交织后比特序完全恢复原序 |
| B-7 | 交织器 + 卷积码联调 | BEC 信道下加入交织器，对比有无交织的译码性能 | 1.5h | B | BEC p=0.1 时，有交织比无交织 BER 降低 ≥ 50% |

### 3.4 测试

| 编号 | 任务 | 详情 | 估计工时 | 负责人 | 完成标准 |
|---|---|---|---|---|---|
| B-8 | 信道编码单元测试 | 覆盖 BSC/BEC 多参数组合，统计 BER 增益 | 2h | B | 至少 6 组参数 (BSC 3 组 + BEC 3 组) 测试通过 |

**成员 B 合计工时：15h**

---

## 阶段四：信道模型 + 系统集成（成员 C）

### 4.1 信道仿真

| 编号 | 任务 | 详情 | 估计工时 | 负责人 | 完成标准 |
|---|---|---|---|---|---|
| C-1 | BSC 信道仿真 | 输入比特流和交叉概率 ε，随机翻转比特并统计实际误码数 | 1h | C | ε=0.05 时实际翻转比例在 0.05±0.01 范围内 |
| C-2 | BEC 信道仿真 | 输入比特流和删除概率 p，随机删除比特，输出有效比特 + 删除位置标记 | 1h | C | p=0.1 时实际删除比例在 0.1±0.01 范围内 |

### 4.2 系统流水线

| 编号 | 任务 | 详情 | 估计工时 | 负责人 | 完成标准 |
|---|---|---|---|---|---|
| C-3 | 接口适配层 | 定义统一的模块接口类 `SourceCodec`, `ChannelCodec`, `Channel`，各模块继承实现 | 1.5h | C | 任何模块替换为其他实现（如汉明码）只需改一行 import |
| C-4 | 主流水线 `main.py` | 实现端到端流程：读图 → 源编码 → 信道编码 → 信道传输 → 信道译码 → 源解码 → 输出 | 2h | C | 命令行参数 `--image --ber --channel --quality` 均可正常工作 |
| C-5 | 比特流打包/拆包 | 源编码输出需加头部（图像尺寸、Q值、Huffman码表），接收端按协议解析 | 2h | C | 编码比特流可保存为 `.bin` 文件，解码端独立读取并正确重建 |

### 4.3 批量实验

| 编号 | 任务 | 详情 | 估计工时 | 负责人 | 完成标准 |
|---|---|---|---|---|---|
| C-6 | 批量实验脚本 | 遍历图集 × 信道类型 × 参数组合，自动运行并记录结果到 CSV | 2h | C | 24 图 × 2 信道 × 3 参数 = 144 组实验可一键运行 |
| C-7 | 实验结果 CSV 导出 | 每组实验记录：图名、信道类型、参数、压缩率、PSNR、SSIM、编码时间、解码时间 | 1h | C | CSV 无缺失字段，供成员 D 直接读取绘图 |

### 4.4 集成测试

| 编号 | 任务 | 详情 | 估计工时 | 负责人 | 完成标准 |
|---|---|---|---|---|---|
| C-8 | 端到端集成测试 | 用 1 张图测试完整流水线，验证无信道时图像可恢复 | 1.5h | C | 无信道损伤时输出图与原始图 PSNR > 35dB，端到端运行无 crash |

**成员 C 合计工时：12h**

---

## 阶段五：性能分析与报告（成员 D）

### 5.1 性能指标计算

| 编号 | 任务 | 详情 | 估计工时 | 负责人 | 完成标准 |
|---|---|---|---|---|---|
| D-1 | PSNR 计算模块 | 输入原始图和重建图，计算逐通道 PSNR + 平均 PSNR | 0.5h | D | 与 `skimage.metrics.peak_signal_noise_ratio` 结果一致 |
| D-2 | SSIM 计算模块 | 实现或调用 SSIM 计算 | 0.5h | D | 与 `skimage.metrics.structural_similarity` 结果一致 |
| D-3 | 算法复杂度分析 | 测量源编解码、信道编解码的 cpu 时间；对关键函数做大 O 复杂度分析并写入报告 | 2h | D | 记录每组实验的时间数据，报告中有复杂度大 O 表格 |

### 5.2 可视化

| 编号 | 任务 | 详情 | 估计工时 | 负责人 | 完成标准 |
|---|---|---|---|---|---|
| D-4 | PSNR 对比曲线 | 不同 BER / Erasure Rate 下的 PSNR 曲线，BSC 和 BEC 分图或同图对比 | 2h | D | 至少输出 3 张高清曲线图，坐标轴标签清晰，图例完整 |
| D-5 | 压缩率-Rate 曲线 | 不同 Q 值对应的压缩率和 PSNR，展现率失真 trade-off | 1.5h | D | 输出 RD 曲线图，标注 Q=10/50/90 三个典型点 |
| D-6 | 复杂度对比图 | 柱状图对比源编码/信道编码/译码各环节耗时占比 | 1h | D | 图可直观看出性能瓶颈模块 |

### 5.3 英文报告

| 编号 | 任务 | 详情 | 估计工时 | 负责人 | 完成标准 |
|---|---|---|---|---|---|
| D-7 | Abstract + Introduction | 背景、问题定义、项目目标 | 1h | D | 各 100~200 words |
| D-8 | System Design | 系统框图、源编码方案、信道编码方案、信道模型描述 | 2h | D | 有清晰的系统架构图 |
| D-9 | Experiments & Results | 实验设置说明 + 结果图表插入 + 分析讨论 | 3h | D | 每张图表均有文字分析 |
| D-10 | Conclusion + References | 结论总结 + 参考文献 | 1h | D | 报告总长度 8~15 页 |
| D-11 | 报告格式排版 + 导出 PDF | LaTeX 或 Word 排版，全英文 | 2h | D | 格式整洁，图表编号规范，无拼写/语法错误 |

### 5.4 最终提交

| 编号 | 任务 | 详情 | 估计工时 | 负责人 | 完成标准 |
|---|---|---|---|---|---|
| D-12 | 打包提交 | 收集可执行文件 + 图集 + 报告 → 压缩包，命名格式：`成员A_成员B_成员C_成员D.zip` | 1h | D | 解压后可直接运行，报告可正常打开 |

**成员 D 合计工时：17.5h**

---

## 总工时统计

| 成员 | 负责模块 | 总工时 |
|---|---|---|
| A | 源编码（DCT + 量化 + 熵编解码） | 14h |
| B | 信道编码（卷积码 + Viterbi + 交织器） | 15h |
| C | 信道模型 + 系统集成 + 批量实验 | 12h |
| D | 性能分析 + 可视化 + 英文报告 + 提交 | 17.5h |
| **合计** | | **58.5h** |


---
