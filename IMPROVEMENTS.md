# 源编码容错性改进记录

## 问题背景

项目实现了一套完整的图像编码传输系统：DCT 源编码 → 卷积信道编码 → BSC/BEC 信道 → Viterbi 译码 → 源解码。但初始版本在信道存在误码时完全无法工作——BSC 1% 误码直接导致程序崩溃或输出纯噪声。

本文档记录了从崩溃到 PSNR 28 dB 的完整改进过程。

---

## 初始问题诊断

### 测试条件

- 图像：kodim01.png (512×768 真彩色)
- 源编码：DCT (Q=75) + Huffman 熵编码
- 信道编码：(2,1,7) 卷积码，Viterbi 硬判决
- 信道：BSC ε=0.01

### 初始表现

```
BSC ε=0.01 → Huffman 解码器抛出 ValueError 崩溃
BSC ε=0.00 → PSNR 32.76 dB（无损正常）
```

### 根因分析

Huffman 编码是变长编码——常见符号用短码、罕见符号用长码。一个 bit 翻转后，解码器找不到码字边界，后续所有符号全部错位。就像一句话删掉一个字，剩下的每个字都往前挪一位，整个句子面目全非。

```
正常码流:  01 | 10110 | 01 | 01 | 10110  →  A  B  A  A  B
1个bit翻:  01 | 00110 | 01 | 01 | 10110  →  A  C  X  X  X  (后面全错)
            ↑ 翻转         ↑ 此处开始所有码字边界偏移
```

Viterbi 译码后仍有 0.5% 残留 BER，500 万 bit 数据中约 2.5 万个 bit 错误，Huffman 变长码完全无法承受。

此外，DC 系数采用差分编码——每个块的 DC 值是相对于前一个块的差值。一个块的 DC 被翻错，同一通道后续所有块的亮度基准全部偏移，形成"多米诺骨牌"效应。

---

## 改进一：去掉 Huffman，改用定长编码

**思路**：Huffman 的压缩增益以脆弱性为代价。去掉变长编码，每个 RLE 符号用固定 2 字节 (int16) 表示。一个 bit 错误最多影响一个数值，不会引发连锁错位。

**改动**：`encoder.py` — RLE 数据直接 int16 序列化，不再经过 Huffman。

**结果**：

| 指标 | 改前 | 改后 |
|---|---|---|
| 压缩率 (Q=75) | ~6x | ~1.9x |
| BSC 1% | 崩溃 | PSNR 4.71 dB |

图片不再崩溃，但 PSNR 很低。RLE 结构 (run, value 对) 仍然脆弱——错误的 run 值把块内系数位置打乱。

---

## 改进二：去掉 DC 差分编码

**思路**：DC 系数存绝对值，每个块完全独立。一个块的 DC 损坏，不影响其他任何块。

**改动**：`encoder.py` / `decoder.py` — 删除 `prev_dc` 累加逻辑。

**结果**：BSC 1% PSNR 从 4.71 → 6.82 dB。

---

## 改进三：RLE 解码容错强化

**思路**：RLE 解码时，检查 run 值的合理性。一个块最多 63 个 AC 系数，run 值若 ≤0 或 ≥64，说明数据已损坏，立即停止该块解码（剩余系数归零）。

**改动**：`encoder.py` 的 `_rle_decode()` — 加边界检查，异常 run 值触发块级回退。

**结果**：块内错误不再扩散。但 RLE 数据是所有块串在一起的，坏块的 RLE 解析错误仍可能影响后续块的数据读取。

---

## 改进四：块级独立编址

**思路**：编码时为每个块记录它的 RLE 数据长度，存入 header。解码时按偏移量直接跳到每个块的数据起始位置——坏块的数据被跳过，不影响好块。

**改动**：
- `encoder.py` — 记录每块 RLE int 个数到 `block_lengths`
- `decoder.py` — 用累计偏移量逐块定位，坏块填零跳过

**架构变化**：

```
改前: [块0数据][块1数据][块2数据]... → 一个坏了，后面全丢
改后: header: [块0长度|块1长度|块2长度|...]
      数据:   [块0数据][块1数据][块2数据]... → 每个块独立寻址
```

---

## 改进五：Nx 重复编码 + 多数投票

**思路**：商用通信系统的级联编码——内层卷积码纠正大部分错误，外层重复编码兜底。每个块的数据重复 N 遍，解码端逐 byte 逐 bit 进行 N 选 ⌊N/2⌋+1 投票，消除卷积码残留的零星错误。

**原理**：

```
单一 bit 错误概率: p = 0.01 (Viterbi 残留 BER)
3x 重复后错误概率: ≈ 3p² = 0.0003
5x 重复后错误概率: ≈ 10p³ = 0.00001
```

**改动**：
- `encoder.py` — 每个块的 Huffman 字节序列重复 `REPEAT` 次
- `decoder.py` — 读取 N 份副本，逐 bit 多数投票，再 Huffman 解码

**投票机制**：

```
3份副本:  [10110010] [10110010] [10110011]  ← 第3份最低位被翻错
          bit-by-bit:  1  0  1  1  0  0  1  0    ← 2:1 多数即为原值
```

---

## 改进六：加回 Huffman（带块级保护）

**思路**：有了块级编址和 Nx 重复的容错保护后，可以将 Huffman 加回。Huffman 在单块内的错误传播被块级编址限制，重复编码则将残留错误率降到极低。

**改动**：
- `encoder.py` — 全局 Huffman 表，每块独立 Huffman 编码 + Nx 重复
- `decoder.py` — Nx 多数投票后，每块独立 Huffman 解码

---

## 效果对比

| 版本 | 压缩率 | BSC 1% PSNR | BSC 0.5% PSNR | BEC 5% PSNR | 耗时 |
|---|---|---|---|---|---|
| 原始 (Huffman) | 6.0x | 崩溃 | 崩溃 | 崩溃 | — |
| 去 Huffman | 1.9x | 4.71 dB | 5.01 dB | — | — |
| +去 DC 差分 +RLE 容错 | 1.9x | 6.82 dB | — | — | — |
| +块级编址 | 1.9x | 6.74 dB | — | — | — |
| +3x 重复 (无 Huffman) | 0.6x | 15.23 dB | 20.39 dB | 29.44 dB | 170s |
| +5x 重复 (无 Huffman) | 0.4x | 25.35 dB | — | — | 376s |
| **+Huffman +5x 重复（最终）** | **1.2x** | **28.02 dB** | — | — | **66s** |

---

## 核心教训

1. **变长编码 + 噪声信道 = 灾难**：Huffman 等熵编码必须配合块级同步机制使用
2. **块间依赖需要打破**：DC 差分、RLE 串行解析等跨块依赖是错误放大器
3. **级联编码是标准解法**：内层卷积码 + 外层重复/分组码，商用系统 (4G/5G/WiFi) 均采用此架构
4. **数据量 ↔ 容错是可调参数**：`REPEAT` 一个变量即可在速度和鲁棒性之间滑动调节

---

## 可调参数

```python
# encoder.py 顶部
REPEAT = 5   # 1=最快/最脆 3=均衡 5=最强/稍慢
```

解码器自动适配重复次数，无需额外修改。
